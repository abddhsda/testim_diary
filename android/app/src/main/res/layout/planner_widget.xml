<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/widget_background">

    <!-- Заголовок — тап открывает планировщик в приложении -->
    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📋 Планы на сегодня"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/widget_text"
        android:layout_marginBottom="8dp"
        android:paddingBottom="8dp" />

    <!-- Скроллируемый список планов с галочками -->
    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Показывается если планов нет -->
    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="Нет планов на сегодня"
        android:textSize="13sp"
        android:textColor="@color/widget_subtext"
        android:gravity="center"
        android:visibility="gone" />

</LinearLayout>
